# Backend API (Express + Supabase service role)
NEXT_PUBLIC_API_URL=http://localhost:3001

# Optional: keep for future auth / realtime (data flows through backend for now)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-anon-key

# Must match backend SUPABASE_ANON_KEY
